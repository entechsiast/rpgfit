{
  "default": {
    "require": [
      "tests/step-definitions/**/*.ts",
      "tests/support/**/*.ts"
    ],
    "paths": ["tests/features/**/*.feature"],
    "requireModule": ["ts-node/register"],
    "format": ["progress"]
  }
}
